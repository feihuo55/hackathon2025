[project]
# List of environment variables to be provided by each user to use the app.
user_env = []

# Duration (in seconds) during which the session is saved when the connection is lost
session_timeout = 3600

# Duration (in seconds) of the user session expiry
user_session_timeout = 1296000  # 15 days

# Enable third parties caching (e.g., LangChain cache)
cache = false

# Whether to persist user environment variables (API keys) to the database
persist_user_env = false

# Whether to mask user environment variables (API keys) in the UI
mask_user_env = false

# Authorized origins
allow_origins = ["*"]

[features]
# Process and display HTML in messages
unsafe_allow_html = false

# Process and display mathematical expressions
latex = false

# Autoscroll new user messages at the top of the window
user_message_autoscroll = true

# Automatically tag threads with the current chat profile
auto_tag_thread = true

# Allow users to edit their own messages
edit_message = true

# Allow users to share threads
allow_thread_sharing = false

[features.slack]
reaction_on_message_received = false

[features.spontaneous_file_upload]
    enabled = true
    accept = ["*/*"]
    max_files = 20
    max_size_mb = 500

[features.audio]
    enabled = false
    sample_rate = 24000

[features.mcp]
    enabled = false

[features.mcp.sse]
    enabled = true

[features.mcp.streamable-http]
    enabled = true

[features.mcp.stdio]
    enabled = true
    allowed_executables = [ "npx", "uvx" ]

[UI]
# Name of the assistant
name = "Custody Bank AI"

# Theme setting
default_theme = "light"

# Layout
layout = "default"

# Sidebar default state
default_sidebar_state = "closed"

# Description for HTML meta tags
description = "Custody Bank AI Automation Platform - Intelligent fund management assistant"

# Chain of Thought display mode
cot = "hidden"

# Custom CSS file (served from public folder)
custom_css = "/public/theme.css"

# Alert box style
alert_style = "modern"

# Logo and avatar
logo_file_url = ""
default_avatar_file_url = ""

# Custom theme colors - Blue theme
[UI.theme]
    font_family = "Inter, sans-serif"
    [UI.theme.light]
        background = "#ffffff"
        paper = "#f8fafc"
        [UI.theme.light.primary]
            main = "#2563eb"
            dark = "#1d4ed8"
            light = "#60a5fa"

[meta]
generated_by = "2.9.2"
